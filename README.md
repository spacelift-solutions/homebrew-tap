# Spacelift Solutions Homebrew tap

Homebrew formulae and casks for tools built by the Spacelift Solutions team.

```sh
brew install spacelift-solutions/tap/liftoff
```

Everything in `Casks/` is generated on release — edit it upstream, not here.
